server:
  port: 9090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    password: tiger
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
